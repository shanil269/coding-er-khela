import { AuthHttpIntercepter } from './auth-http-intercepter';

describe('AuthHttpIntercepter', () => {
  it('should create an instance', () => {
    expect(new AuthHttpIntercepter()).toBeTruthy();
  });
});
